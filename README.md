# JavaAcript Important Notes

## Number(x)
Convert strings(number) to number. 123(string) = 123(number).

## x.toString()
Convert number to string. 123(number) = 123(string).

## x.split('')
Convert string to array.

## x.join('')
Convert array to string.
